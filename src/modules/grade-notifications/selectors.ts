import style from './style.css';

const selectors = {
  notificationToggleButton: style.locals['notification-toggle-button'],
  notificationToggleButtonEnabled: style.locals['notification-toggle-button-enabled'],
  progressHeaderBubble: style.locals['progress-header-bubble'],
  viewAssignmentsButton: style.locals['view-assignments-button'],
  clearNotificationButton: style.locals['clear-notification-button'],
  modalTable: {
    pointsHeader: style.locals['modal-table-points-header'],
  },
};

export default selectors;
